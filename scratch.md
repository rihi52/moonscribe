
# Scratch

``` dart
monster == null
    ? const Text("Loading...")
    : Text(monster!.name),
monster == null
    ? const Text("Loading...")
    : Text(monster!.size),
```


## Tables
### Players
id
name
class
level?
hitPoints?
armorClass?
originalCampaign
```